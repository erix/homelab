# TEMPLATE - Do not commit with real values!
# 
# Option 1: Create secret directly (recommended)
# kubectl create secret generic op-service-account \
#   --from-literal=token="YOUR_1PASSWORD_SERVICE_ACCOUNT_TOKEN" \
#   --namespace=default
#
# Option 2: Seal this secret with kubeseal
# 1. Replace YOUR_TOKEN_HERE with actual token
# 2. Run: cat op-service-account-secret.yaml.template | kubeseal --format yaml > op-service-account-sealed.yaml
# 3. Apply: kubectl apply -f op-service-account-sealed.yaml
# 4. Delete this template file (don't commit real tokens!)

apiVersion: v1
kind: Secret
metadata:
  name: op-service-account
  namespace: default
type: Opaque
stringData:
  token: "YOUR_TOKEN_HERE"
