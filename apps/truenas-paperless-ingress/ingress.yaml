apiVersion: v1
kind: Service
metadata:
  name: truenas-paperless-service
  namespace: default
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 30070

---
apiVersion: v1
kind: Endpoints
metadata:
  name: truenas-paperless-service
  namespace: default
subsets:
  - addresses:
      - ip: 192.168.1.179
    ports:
      - port: 30070

---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: truenas-paperless-route
  namespace: default
spec:
  entryPoints:
    - web
    - websecure
  routes:
  - match: Host(`paperless.erix-homelab.site`)
    kind: Rule
    services:
    - name: truenas-paperless-service
      port: 80
  tls:
    secretName: erix-homelab-site-tls
    domains:
    - main: erix-homelab.site
