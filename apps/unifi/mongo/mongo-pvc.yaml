apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
  namespace: network
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  volumeName: mongodb-sts
  resources:
    requests:
      storage: 10Gi 
