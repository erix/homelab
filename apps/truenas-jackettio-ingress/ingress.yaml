apiVersion: v1
kind: Service
metadata:
  name: truenas-jackettio-service
  namespace: default
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 4000

---
apiVersion: v1
kind: Endpoints
metadata:
  name: truenas-jackettio-service
  namespace: default
subsets:
  - addresses:
      - ip: 192.168.1.179
    ports:
      - port: 4000

---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: truenas-jackettio-service-route
  namespace: default
spec:
  entryPoints:
    - web
    - websecure
  routes:
  - match: Host(`jackettio.erix-homelab.site`)
    kind: Rule
    services:
    - name: truenas-jackettio-service
      port: 80
  tls:
    secretName: erix-homelab-site-tls
    domains:
    - main: erix-homelab.site
