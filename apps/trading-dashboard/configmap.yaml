apiVersion: v1
kind: ConfigMap
metadata:
  name: trading-dashboard-config
  namespace: default
data:
  CP_GATEWAY_URL: "https://ibkr.erix-homelab.site"
  CP_GATEWAY_PUBLIC_URL: "https://ibkr.erix-homelab.site"
  IBKR_ACCOUNT_ID: "U7525143"
  DB_PATH: "/data/db/positions.db"
  POSITIONS_META_FILE: "/trading/positions_meta.json"
  PLAYBOOK_FILE: "/data/playbook.md"
  BACKUP_ENABLED: "true"
  BACKUP_DIR: "/data/db/backups"
  DASHBOARD_AUTH_REQUIRED: "true"
  DASHBOARD_COOKIE_SECURE: "true"
  RECONCILIATION_ENABLED: "true"
  RECONCILIATION_APPLY_ENABLED: "false"
  RECONCILIATION_APPLY_MAX_CONTRACTS: "100"
  RECONCILIATION_APPLY_EXACT_CLOSE_ENABLED: "false"
  RECONCILIATION_APPLY_PARTIAL_CLOSE_ENABLED: "false"
  RECONCILIATION_APPLY_SAME_EXPIRATION_ROLL_ENABLED: "false"
  RECONCILIATION_APPLY_ROLL_CONTINUATION_ENABLED: "false"
