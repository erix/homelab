apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-data-pvc
  labels:
    app: n8n
  annotations:
    longhorn.io/volume: n8n-data
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
