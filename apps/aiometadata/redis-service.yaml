apiVersion: v1
kind: Service
metadata:
  name: aiometadata-redis
  namespace: stremio
  labels:
    app: aiometadata-redis
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: aiometadata-redis
