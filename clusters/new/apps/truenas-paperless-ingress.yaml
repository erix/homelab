apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: truenas-paperless-ingress
  namespace: flux-system
spec:
  interval: 10m
  path: ../../apps/truenas-paperless-ingress
  prune: true
  targetNamespace: default
  sourceRef:
    kind: GitRepository
    name: flux-system
