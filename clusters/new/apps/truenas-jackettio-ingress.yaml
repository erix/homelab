apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: truenas-jackettio-ingress
  namespace: flux-system
spec:
  interval: 10m
  path: ../../apps/truenas-jackettio-ingress
  prune: true
  targetNamespace: default
  sourceRef:
    kind: GitRepository
    name: flux-system
